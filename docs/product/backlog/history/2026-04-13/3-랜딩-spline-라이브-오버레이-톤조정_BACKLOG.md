## 차수 1

### 작업내용

- 랜딩 히어로에서 Spline 라이브 씬이 보일 때 좌측 카피 영역이 과하게 검게 덮이는 원인을 확인한다.
- fallback 상태와 live 상태를 구분할 수 있게 필요한 최소 상태 연결을 추가한다.
- live 상태에서는 검은 패널을 제거하고, 가독성을 유지하는 약한 glass 오버레이로 조정한다.

### 논의 필요

- 없음

### 선택지

- `.heroCopyShell` 자체를 항상 투명하게 바꾼다.
- Spline 라이브 상태일 때만 overlay 톤을 낮추고 fallback 상태는 기존 대비를 유지한다.

### 추천

- Spline 라이브 상태일 때만 overlay 톤을 낮추고 fallback 상태는 기존 대비를 유지한다.
- 실제 문제를 직접 겨냥하면서 fallback readability까지 보존할 수 있다.

### 사용자 방향
