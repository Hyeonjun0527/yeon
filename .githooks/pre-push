#!/bin/sh

set -eu

repo_root=$(git rev-parse --show-toplevel)

if ! command -v pnpm >/dev/null 2>&1; then
  echo "pnpm을 찾을 수 없어 pre-push 검증을 중단합니다." >&2
  exit 1
fi

cd "$repo_root"

echo "[pre-push] pnpm lint"
pnpm lint

echo "[pre-push] pnpm typecheck"
pnpm typecheck
