# Public API Surface

Put public HTTP endpoints here.

Use this folder for capabilities that must be shared by:

- web client code
- mobile client code
- external integrations

Do not hide shared product capabilities behind Server Actions only.
