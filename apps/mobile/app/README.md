# Expo Router

Suggested route split:

- `(auth)`: sign-in, onboarding, recovery
- `(tabs)`: primary logged-in navigation

Keep route files here and push reusable logic into `src/features` and `src/services`.
