# 공개 체크인 Juso 오류 노출 보정 백로그

## 1차

- 작업내용
  - Juso 검색/좌표 응답의 `juso: null` 케이스를 스키마에서 허용한다.
  - `E0001` 승인키 오류가 `응답 형식 오류`가 아니라 설정 안내 메시지로 바로 노출되게 수정한다.
  - 서비스 테스트에 `juso: null` 실응답 케이스를 추가한다.
- 논의 필요
  - 없음
- 선택지
  - A. null을 빈 배열로 정규화한 뒤 오류코드 우선 처리
  - B. 스키마 에러를 그대로 유지
- 추천
  - A. 실제 사용자에게 원인 메시지를 정확히 보여줘야 한다.
- ## 사용자 방향
